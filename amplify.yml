version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd my-app
        - npm ci
    build:
      commands:
        - cd my-app
        - npm run build
  artifacts:
    baseDirectory: my-app/.next
    files:
      - '**/*'
  cache:
    paths:
      - my-app/node_modules/**/*